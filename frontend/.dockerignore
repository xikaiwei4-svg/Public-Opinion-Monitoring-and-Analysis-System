# frontend/.dockerignore
# 多阶段构建需要源文件，只排除绝对不需要的内容

# 依赖（由 npm ci 重新安装）
node_modules/

# 环境变量（不提交敏感信息，保留 .env.production）
.env
.env.local
.env.*.local

# 构建产物（多阶段构建会重新生成，排除本地已构建的避免层膨胀）
dist/

# 日志
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
